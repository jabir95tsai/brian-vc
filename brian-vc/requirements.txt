# Canonical standalone Python dependencies for all three bundled Skills.
# Codex/ChatGPT workflows should prefer the managed Documents, Presentations,
# Spreadsheets, and PDF runtimes when they are available.
python-docx>=1.2,<2
python-pptx>=1.0,<2
pypdf>=5,<7
PyMuPDF>=1.24,<2

# Transitional dependency used by prospectus-extractor. New evaluator workbook
# authoring uses the managed @oai/artifact-tool runtime instead.
openpyxl>=3.1,<4
